<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.book.manager.dao.RecommendationMapper">

    <resultMap id="RecommendationMap" type="com.book.manager.entity.Recommendation">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="book_id" property="bookId"/>
        <result column="score" property="score"/>
        <result column="source" property="source"/>
        <result column="computed_at" property="computedAt"/>
    </resultMap>

    <delete id="deleteByUser">
        DELETE FROM recommendation WHERE user_id = #{userId}
    </delete>

    <insert id="insertBatch">
        INSERT INTO recommendation (user_id, book_id, score, source, computed_at)
        VALUES
        <foreach collection="list" item="r" separator=",">
            (#{r.userId}, #{r.bookId}, #{r.score}, #{r.source}, NOW(6))
        </foreach>
    </insert>

    <select id="listByUser" resultMap="RecommendationMap">
        SELECT * FROM recommendation
        WHERE user_id = #{userId}
        ORDER BY computed_at DESC
        <if test="limit != null">LIMIT #{limit}</if>
    </select>

</mapper>
